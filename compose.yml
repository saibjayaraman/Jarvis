services:
  jarvis:
    build: .
    ports:
      - "3000:3000"
      - "11434:11434"
    volumes:
      - ./:/app
      - /app/node_modules
    extra_hosts:
      - "host.docker.internal:host-gateway"